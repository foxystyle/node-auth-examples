{{#if flashSuccess}}
  <div style="background: green; color: white">{{flashSuccess}}</div>
{{/if}}
{{#if flashError}}
  <div style="background: red; color: white">{{flashError}}</div>
{{/if}}
<h1>Hello world!</h1>
<hr />
{{#if isAuthenticated}}
  <strong>Hello, {{user.email}},</strong> <br /> perhaps you could visit some other pages:
  <a href="/pass/test">Such as this one</a>, or logout by pressing this button:
  <p></p>
  <form method="post" action="/pass/logout">
    <input type="submit" value="logout" />
  </form>
{{else}}
  <h2>Login</h2>
  <form method="post" action="/pass/login">
    <input type="email" name="email" />
    <input type="password" name="password" />
    <input type="submit" value="Login" />
  </form>

  <div>
    ...and you will have access to links <a href="/pass/test">such as this one</a>.
  </div>
{{/if}}
<hr />
